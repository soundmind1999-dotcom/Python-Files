def check_if_first_number_can_be_divided_by_second_number (first_number, second_number):
    check_division = first_number / second_number
        return check_division


def divide_until_it_gets_to_zero(first_number, second_number):
    if second_number < 2 and > 36
        return -1

    if first_number < 0
        return 0

    count = 0
    while first_number > 0:
        first_number // second_number
        count += 1
            return count
    





























 















